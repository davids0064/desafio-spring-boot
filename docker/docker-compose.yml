version: '3.8'

services:
  nuevospa:
    image: nuevo-spa:latest
    build:
      context: .
      dockerfile: Dockerfile.nuevospa
    ports:
      - "8080:8080"
    networks:
      - nuevospa-net
      
networks:
  nuevospa-net:
    driver: bridge